import "./App.css";
import Video from "./Video";

function App() {
  return (
    <div className="App">
      <div className="app_videos">
        <Video url={}
          description={ }
          username={ }
          song={ }
          likes={ }
          comments={ }
          shares={ }/>
        <Video url={}
          description={ }
          username={ }
          song={ }
          likes={ }
          comments={ }
          shares={ }/>
      </div>
    </div>
  );
}

export default App;
